import React from 'react'
import Map from './Map'

var MainPage = React.createClass({
  render: function() {
    return (
      <div>
        <Map />
      </div>
    );
  }
});

export default MainPage;